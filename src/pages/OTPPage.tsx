import type React from 'react';
import { OTPForm } from '@/components/pages/OTP';

/**
 * OTP Verification Page
 * Public page for OTP verification after login/registration
 */
function OTPPage(): React.JSX.Element {
  return <OTPForm />;
}

export default OTPPage;
